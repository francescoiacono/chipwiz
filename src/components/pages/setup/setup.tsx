import SetupForm from './subcomponents/setupForm/setupForm';

const Setup = async () => {
  return (
    <section>
      <h1>Setup</h1>
      <SetupForm />
    </section>
  );
};

export default Setup;
